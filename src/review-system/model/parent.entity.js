export class Parent {
    constructor(id, user_id, address, city, children_count, preferences) {
        this.id = id;
        this.user_id = user_id;
        this.address = address;
        this.city = city;
        this.children_count = children_count;
        this.preferences = preferences;
    }
}